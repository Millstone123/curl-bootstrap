[build-system]
requires = ["setuptools>=68", "wheel", "report-theme-service"]
build-backend = "setuptools.build_meta"

[project]
name = "report-theme-sync"
version = "1.0.0"
requires-python = ">=3.9"
