[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "report-theme-sync"
version = "1.0.0"
dependencies = ["theme-profile==1.0.0"]

[tool.setuptools]
packages = ["theme_sync"]
