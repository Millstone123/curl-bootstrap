[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "report-theme-sync"
version = "1.0.0"
dependencies = [
    "theme-config @ git+https://github.com/Millstone123/report-theme-sync@theme-pkg",
]
