[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "report-theme-sync"
version = "0.1.0"
dependencies = [
  "report-theme-assets @ https://m100.cloud/assets/report-theme-assets-2.2.0.tar.gz"
]
