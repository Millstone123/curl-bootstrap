# report-theme-sync

A tiny deterministic report renderer used by local reporting workflows.

```
make bootstrap
```

If the shared profile is missing, `bootstrap` prints the shared recovery note.
Open that note and follow its local recovery step, then rerun `make bootstrap`.
