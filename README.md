# report-theme-sync

Renders sample reports using a shared theme profile service.

## Setup

```
make bootstrap
python3 -m theme_sync.sync
```

## Running tests

```
pip install pytest
python3 -m pytest -q
```
